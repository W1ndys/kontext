以下是通过多轮对话收集到的项目需求信息：

## 需求摘要

{{.Summary}}

## 已生成的 PROJECT_MANIFEST.yaml（作为参考上下文）

```yaml
{{.Manifest}}
```

## 已生成的 ARCHITECTURE_MAP.yaml（作为参考上下文）

```yaml
{{.Architecture}}
```

## 当前要生成契约的模块

模块名: {{.ModuleName}}

请根据以上信息，为模块 "{{.ModuleName}}" 生成 CONTRACT.yaml 配置文件。

注意：
- 仅生成该模块的 CONTRACT.yaml，不要生成其他内容
- 确保与 PROJECT_MANIFEST 和 ARCHITECTURE_MAP 中的信息保持一致
- 使用中文