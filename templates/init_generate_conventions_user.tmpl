以下是通过多轮对话收集到的项目需求信息：

## 需求摘要

{{.Summary}}

## 已生成的 PROJECT_MANIFEST.yaml（作为参考上下文）

```yaml
{{.Manifest}}
```

## 已生成的 ARCHITECTURE_MAP.yaml（作为参考上下文）

```yaml
{{.Architecture}}
```

请根据以上信息，生成 CONVENTIONS.yaml 配置文件。

注意：
- 仅生成 CONVENTIONS.yaml，不要生成其他文件
- 确保与 PROJECT_MANIFEST 中的技术栈信息保持一致
- 编码规范应基于对话中讨论的项目特点
- 使用中文