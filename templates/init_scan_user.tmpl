请根据以下项目源码信息，生成三个 YAML 配置文件。

## 项目目录树

```
{{.DirectoryTree}}
```

{{if .ConfigFiles}}
## 依赖与配置文件

{{range $path, $content := .ConfigFiles}}
### {{$path}}

```
{{$content}}
```

{{end}}
{{end}}

{{if .KeyFileContents}}
## 重点源码文件（完整内容）

以下是 AI 识别的核心文件，包含完整实现细节：

{{range $path, $content := .KeyFileContents}}
### {{$path}}

```
{{$content}}
```

{{end}}
{{end}}

{{if .OtherFileSummaries}}
## 其他源码文件（仅签名概要）

以下是其他源码文件的概要信息（包含 import 和函数签名）：

{{range $path, $summary := .OtherFileSummaries}}
### {{$path}}

```
{{$summary}}
```

{{end}}
{{end}}

{{if .CodeSnippets}}
## 关键源码片段（每个文件前 50 行）

{{range $path, $content := .CodeSnippets}}
### {{$path}}

```
{{$content}}
```

{{end}}
{{end}}

请根据以上信息分析项目并生成配置。