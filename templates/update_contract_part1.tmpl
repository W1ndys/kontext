目标模块：{{.ModuleName}}

当前契约 YAML：
{{.CurrentYAML}}

检测到的变更：
{{.Changes}}

当前代码摘要：
{{.CodeSummary}}

请在现有契约基础上做最小修改，只生成以下四个顶级字段：module、owns、not_responsible_for、depends_on。
不要生成 public_interface 和 modification_rules（将在下一步单独生成）。
如果这是新模块，请为这四个字段生成合理内容。
如果模块已删除且不应再保留，请返回 {"content": ""}。

以 JSON 格式返回，结构为 {"content": "部分 CONTRACT YAML 文本"}。content 字段的值必须严格符合以下 schema：

```yaml
module:
  name: "{{.ModuleName}}"
  path: "模块路径，例如 src/updater 或 cmd"
  purpose: |
    模块核心职责描述

owns:
  - "该模块负责的功能点"

not_responsible_for:
  - "该模块明确不负责的内容"

depends_on:
  - module: "依赖模块名"
    reason: "依赖原因"
```

注意：
- `module` 必须是对象，不能写成 `module: {{.ModuleName}}`
- 必须使用 `owns`，不能改成 `responsibilities`
- 不要包含 public_interface 和 modification_rules
