目标模块：{{.ModuleName}}

已生成的契约前面部分（module、owns、not_responsible_for、depends_on、public_interface）：
{{.PrecedingYAML}}

当前契约 YAML（完整版，供参考）：
{{.CurrentYAML}}

请基于以上信息，只生成 `modification_rules` 这一个顶级字段。
不要生成 module、owns、not_responsible_for、depends_on、public_interface。
请在现有契约基础上做最小修改。如果这是新模块，请生成合理内容。

以 JSON 格式返回，结构为 {"content": "部分 CONTRACT YAML 文本"}。content 字段的值必须严格符合以下 schema：

```yaml
modification_rules:
  - rule: "修改该模块时必须遵守的规则"
    reason: "原因"
```

注意：
- 必须使用 `modification_rules`，不能改成 `notes`
- 只输出 modification_rules，不要输出其他字段
